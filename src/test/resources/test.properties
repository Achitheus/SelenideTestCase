# Use chrome profile to avoid captcha
user.data.dir = ${USER_DATA}
profile.dir = profileForTests

# Properties from active maven profile
maven.profile = ${maven.profile}
use.browser.profile = ${use.browser.profile}
headless = ${headless}
use.selenoid = ${use.selenoid}